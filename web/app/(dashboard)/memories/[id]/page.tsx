'use client';

import { Title, Container } from '@mantine/core';

export default function MemoryDetailPage() {
  return (
    <Container>
      <Title order={2}>Memory Detail</Title>
    </Container>
  );
}
