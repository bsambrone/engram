'use client';

import { Title, Container } from '@mantine/core';

export default function PersonDetailPage() {
  return (
    <Container>
      <Title order={2}>Person Detail</Title>
    </Container>
  );
}
